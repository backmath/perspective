defmodule PerspectiveTest do
  use ExUnit.Case

  # test "Reload Perspective.EventChainBackup on boot"
  # test "setup subscriptions for domain nodes"
  # test "setup subscriptions for request registry (name needs help)"
end
